# Bordeaux golden demo NATS configuration.
# TCP is used by Core/S1 services; WebSocket is used by the browser C2 operational stream.
port: 4222
jetstream: enabled

websocket {
  port: 9222
  no_tls: true
}
